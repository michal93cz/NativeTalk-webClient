export class Notice {
  constructor(
    title?: string,
    description?: string,
    language?: string,
    city?: string,
    date?: string,
    _id?: number,
    owner?: string,
    interested_users?: [ string ],
    selected_user?: string,
    created_at?: string,
    district?: string,
    country?: string
  ) {  }
}
